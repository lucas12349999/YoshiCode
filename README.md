# YoshiCode-1B
1B-param code assistant (TinyLlama base) — domain-adaptive pretraining + SFT, served via vLLM.

## Quick start
- Local (no GPU): create jsonl pilot, dry-run scripts.
- Cloud GPU: run training/run_pretrain.sh then training/run_sft.sh, serve with serving/serve_vllm.sh.


